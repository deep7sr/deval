# Copy to .env and fill in. For the local dev/demo, a Groq key is all you need.
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxx

# A LiteLLM virtual key for clients/eval to authenticate to the proxy.
# In dev this can be any value unless you enable auth on the proxy.
LITELLM_VIRTUAL_KEY=sk-1234
